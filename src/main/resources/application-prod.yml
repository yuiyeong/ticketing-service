spring:
    config:
        activate:
            on-profile: prod
    jpa:
        hibernate:
            ddl-auto: validate

logging:
    request-response:
        enabled: false

config:
    scheduler:
        enabled: true

decorator:
    datasource:
        p6spy:
            enable-logging: false
